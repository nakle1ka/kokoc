'use client'
import { MainButton } from "@/components/ui/mainButton/mainButton";

export default function Home() {
  return (
    <div>
      <MainButton text="te" onClick={() => {}}></MainButton>
    </div>
  );
}
